# Flutter Frontend

A basic Flutter project scaffold.

## Getting Started

Since this was scaffolded manually (Flutter CLI was not found in the environment), you might need to run the following once you have Flutter installed:

```bash
flutter pub get
flutter run
```
